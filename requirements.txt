Flask==3.0.3
gunicorn==22.0.0
Pillow==10.4.0
Werkzeug==3.0.3
# Markdown rendering for the Scraps feature. bleach sanitizes the rendered
# HTML before it's ever marked safe for the template — Markdown allows raw
# HTML embedding by default, and Scraps is admin-authored but publicly
# viewable, so this closes that off rather than trusting it implicitly.
Markdown==3.7
bleach==6.1.0
# Pure-Python IANA timezone database. python:3.12-slim doesn't ship the OS-level
# tzdata package, so zoneinfo.ZoneInfo("Australia/Sydney") etc. would otherwise
# fail to resolve — this makes it work regardless of the base image.
tzdata
